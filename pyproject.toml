[project]
name = "nanofinetune"
version = "0.1.0"
description = "Single-H100 fine-tuning speedrun on Modal."
readme = "README.md"
requires-python = ">=3.12,<3.13"
dependencies = [
    "modal>=1.4",
]

[tool.uv]
package = false
